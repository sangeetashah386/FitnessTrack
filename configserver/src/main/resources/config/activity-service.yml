#spring.application.name=activityService
server:
  port: 8082

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl_auto: update
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

  application:
    name: activity-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka
  instance:
#    hostname: eureka-server
    prefer-ip-address: false
    instance-id: ${spring.application.name}:${server.port}

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking
  delete:
    queue: activity.deleted.queue
    routingKey: activity.deleted


#rabbitmq.exchange.name: fitness.exchange
#rabbitmq.queue.name: activity.queue
#rabbitmq.routing.key: activity.tracking
#rabbitmq.delete.queue: activity.deleted.queue
#rabbitmq.delete.routing.key: activity.deleted